interface Shape:
    f64 area(self);

struct Circle:
    f64 radius;

methods Circle:
    f64 area(self):
        return 3.14 * self.radius * self.radius;

i32 main():
    Circle c;
    c.radius = 5.0;
    Shape s = c;
    f64 a = s.area();
    if (a > 78.0):
        return 1;
    return 0;
